import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function FamiliesScreen() {
  return (
    <PlaceholderScreen
      title="Families"
      subtitle="Family groups and members"
      icon="person-add-outline"
      message="Family records and their members will appear here once the backend is connected."
    />
  );
}
