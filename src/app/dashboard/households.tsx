import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function HouseholdsScreen() {
  return (
    <PlaceholderScreen
      title="Households"
      subtitle="Household units per purok"
      icon="home-outline"
      message="Household units and their addresses will appear here once the backend is connected."
    />
  );
}
